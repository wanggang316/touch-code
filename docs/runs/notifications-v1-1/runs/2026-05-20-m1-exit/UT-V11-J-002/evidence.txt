Probes executed
===============

1) tc status (same as J-001):
   server             0.2.3
   uptime             32887.8s
   connectedClients   0
   -> The running app is release v0.2.3, not the M1 feature build.

2) Live notifications.json shape (same probe):
   $ jq 'if type == "array" then {top_type:"array", count:length}
         else {top_type:"object", keys:keys, version:.version,
               entries_type:(.entries|type),
               entries_count:(.entries|length)} end' \
       ~/.config/touch-code/notifications.json
   {
     "top_type": "array",
     "count": 500
   }
   File size: 315277 bytes; mtime 2026-05-20 19:24.
   The file is in the v1.0 bare-array shape. If the M1 build were
   actively writing this file, AC-V11-J2 says we would expect to see
   envelope shape after the first save. We do not. This is consistent
   with the running app being pre-M1, NOT with the M1 build being
   broken — we are simply not running the build under test.

3) M1 feature build artefacts:
   - apps/mac/.build/  -> only `ghostty/` and `release/` (a v0.2.3
     xcarchive dated 2026-05-06), no touch-code.app from the M1 branch.
   - apps/mac/build/   -> empty of touch-code.app.
   - ~/Library/Developer/Xcode/DerivedData/touch-code-*  -> no
     touch-code.app product (Xcode index-build is running but no
     archived/built product is present).
   - /Applications/touch-code.app -> does not exist.

What this run cannot tell you
=============================
- Whether the M1 build, when handed an existing bare-array
  notifications.json with N entries (some unread), correctly:
  (a) decodes the bare array on launch and surfaces the rows in the
      status-bar bell popover with the right unread badge count;
  (b) on the next save, writes envelope shape with `.version == 1`
      and `.entries | length == N + 1`.

To re-run for PASS/FAIL
=======================
- Back up the user's real ~/.config/touch-code/notifications.json:
    mv ~/.config/touch-code/notifications.json \
       ~/.config/touch-code/notifications.json.user-backup-2026-05-20
- Seed a fresh 3-entry bare-array fixture (2 unread / 1 read) at
  ~/.config/touch-code/notifications.json (per the brief's documented
  v1.0 entry shape).
- Quit the running release app.
- Build and launch the M1 build:
    make mac-build && make mac-run-app
- Wait for the "App launched" ready signal, open the bell popover and
  confirm it shows 3 rows with unread badge "2".
- Drive one notification-worthy event onto an unfocused pane (e.g.,
  `tc pane send <id> $'\033]9;hello\007'`).
- Wait > 1 s past the inbox debounce.
- Inspect with jq:
    jq 'has("version") and has("entries")
        and (.version == 1)
        and (.entries | length == 4)' \
      ~/.config/touch-code/notifications.json
  Expected: true.
- After the run, restore the user's backed-up file.
