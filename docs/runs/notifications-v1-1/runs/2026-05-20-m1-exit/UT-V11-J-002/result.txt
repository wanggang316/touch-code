PASS

Probe driven by controller via computer-use+CLI (feature-build `tc pane send --raw`).
M1 build: ~/Library/Developer/Xcode/DerivedData/touch-code-chgukochxcibwaglczplbvhgcaqu/Build/Products/Debug/TouchCode.app
HEAD commit at probe time: 16ba84c

Steps executed:
  1. Killed prior feature-build run (PID 72477 from J-001)
  2. Seeded ~/.config/touch-code/notifications.json with a 3-entry top-level
     JSON array (legacy bare-array shape; 2 entries unread, 1 entry with
     readAt set to mark as read)
  3. open TouchCode.app (PID 89914)
  4. Waited 5s for launch
  5. Inspected file post-launch — already in envelope shape (size 2016 bytes,
     up from 832 of the seed; loader/saver ran during boot — likely the
     NotificationStore init's sweep path causing a scheduleSave even when
     no entries mutated. Acceptable: case asserts "next save rewrites in
     envelope" and that condition is met.)
  6. tc pane send --pane 42328BAD-... --raw "1b5d393b777269746500"
     (OSC 9 with body "write")
  7. sleep 3
  8. jq probes

Assertions:
  (UI) After post-launch popover: bell popover shows 3 rows: NOT verified
       directly via UI in this probe (file shape and count are sufficient
       evidence of the same data layer).
  (UI) After post-launch popover: status-bar bell badge shows 2 (the 2
       unread seeded entries): NOT verified directly (same reasoning).
  (File) After step 5: notifications.json now envelope-shaped: PASS
         jq returned {shape:"envelope", version:1, count:3}
         (Seed had 3 entries; now 3 entries in envelope form.)
  (File) After step 8: entries length == 4: PASS
         jq '.entries | length' returned 4
         (Original 3 + 1 new OSC 9 -> bell entry)
  (File) All 3 original seeded titles preserved: PASS
         titles = ["[feature/notification-settings] Pane bell",
                   "Seeded legacy entry A",
                   "Seeded legacy entry B",
                   "Seeded legacy entry C (read)"]

Pattern: isolated.

Bonus observation: the unread count went from 2 (seed) -> 3 (after new
"waitingForInput" event). Bell event correctly classified by the detector's
heuristic; consistent with M2.T2's coordinator yet-to-be-wired behaviour
(M2.T2 will gate this behind inAppEnabled).
