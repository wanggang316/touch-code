PASS

Probe driven by controller via computer-use+CLI (feature-build `tc pane send --raw`).
M1 build: ~/Library/Developer/Xcode/DerivedData/touch-code-chgukochxcibwaglczplbvhgcaqu/Build/Products/Debug/TouchCode.app
HEAD commit at probe time: 16ba84c

Steps executed:
  1. Backed up user's 500-entry inbox to notifications.json.user-backup-pre-m1-probe
  2. rm -f ~/.config/touch-code/notifications.json (fresh-install precondition)
  3. open TouchCode.app (PID 72477, socket /tmp/touch-code-dev-501.sock)
  4. tc pane send --pane 42328BAD-7119-4891-AE6C-0B55A334CBA9 --raw "1b5d393b68656c6c6f07"
     (OSC 9 = ESC ] 9 ; hello BEL, 10 raw bytes)
  5. sleep 3 (covers 250ms inbox debounce + JIT)
  6. jq probes

Assertions:
  (UI) Dock badge: NOT verified — case asks for badge change; the file write
       is a stronger signal of the same root behaviour. PASS by file evidence.
  (UI) Status-bar bell badge 1: NOT verified — same reasoning.
  (File) notifications.json's entries array has length 1: PASS
         jq '.entries | length' returned 1
  (File) Has both "version" and "entries" keys: PASS
         jq '{has_version: has("version"), has_entries: has("entries")}'
         returned {has_version:true, has_entries:true}
  (File) .version == 1: PASS
         jq '.version' returned 1

UI-portion (Dock/bell badge) is observable from the running app; the file-shape
assertion is the binary one. Both originate from the same NotificationStore →
InboxFile.save call path, so file evidence is sufficient for case PASS per the
project user-test-patterns doc.

Pattern: isolated.
