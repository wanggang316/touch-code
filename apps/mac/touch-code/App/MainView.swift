import SwiftUI

struct MainView: View {
  var body: some View {
    Text("touch-code")
      .font(.largeTitle)
      .frame(maxWidth: .infinity, maxHeight: .infinity)
  }
}

#Preview {
  MainView()
}
