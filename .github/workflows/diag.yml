name: diag

on:
  workflow_dispatch:

jobs:
  hello-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ubuntu runner OK"

  hello-macos-14:
    runs-on: macos-14
    steps:
      - run: echo "macos-14 runner OK"

  hello-macos-latest:
    runs-on: macos-latest
    steps:
      - run: echo "macos-latest runner OK"
