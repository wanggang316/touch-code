# pi

Install via `tc skill install --pi` (wraps `pi install git:github.com/wanggang316/touch-code-skill`) or directly via that `pi install` invocation.

TODO: document discovery (pi reads `package.json` `pi.skills`), how to update, and a pointer to a future `pi-notify-touch-code` extension (out of scope for v1).

<!-- STUB: filled in by exec plan 0004 M8 -->
