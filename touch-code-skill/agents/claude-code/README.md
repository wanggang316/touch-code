# Claude Code

Installation path: `~/.claude/skills/touch-code/`. Install with `tc skill install --claude-code`.

TODO: document discovery (Claude Code auto-loads every directory under `~/.claude/skills/`), how to verify (`/skills` inside Claude), and a troubleshooting table.

<!-- STUB: filled in by exec plan 0004 M8 -->
