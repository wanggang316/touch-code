# Targeting and Selectors

Selectors address nodes in the hierarchy tree; UUIDs are also accepted everywhere a selector is accepted.

TODO: document selector forms (Space `1`, Tab `1/2`, Panel `1/2/3`), ambient targeting, `--in`, and creation JSON.

<!-- STUB: filled in by exec plan 0004 M8 -->
